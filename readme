SegmentfaultCatcher is a tool that can print source code, which cause segmentfault, without using gdb or coredump

dependency: tar add2line (linux shell command)

mkdir build
cd build
cmake ..
make

/xx/xx/test 

note: can install execuable to where ever you want, but must use absolute path to execute, or will not work
